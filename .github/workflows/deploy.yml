name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 82.25.105.110
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          fingerprint: SHA256:LQi71ONIKe1Sbp6tNIHdUdmIQLPS3Bn0+1oQWo2jeEk
          script: bash /opt/sites/filim/deploy.sh
          command_timeout: 12m
